import MetricCard from '../MetricCard';
import { Weight } from 'lucide-react';

export default function MetricCardExample() {
  return <MetricCard icon={Weight} label="Peso Atual" value="78 kg" />;
}
